packages/ui — Shared React chat components

Contains the chat UI used by both `apps/web` and the extension iframe embed.

Initial tasks:
- Message list component
- Composer
- Presence indicator
- Report/mute UI

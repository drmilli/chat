# Empty on purpose: `npm run dev` proxies /api to the target in vite.config.ts
# (override with API_PROXY=... to point the proxy elsewhere).
VITE_API_URL=

# See .env.production. Blank here means the mobile wallet option is hidden.
VITE_WALLETCONNECT_PROJECT_ID=

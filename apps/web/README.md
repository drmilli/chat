apps/web — Web application

Run (local dev)
- Install dependencies from repo root (monorepo) or in this package.
- Start dev server (TBD: Vite/CRA) after dependencies are added.

Routes to implement:
- /room/<CA>
- /embed/<CA>

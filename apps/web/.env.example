
# WalletConnect / Reown project id — free from https://cloud.reown.com
# WITHOUT THIS, MOBILE USERS CANNOT CONNECT AT ALL: there is no extension on a
# phone to inject a wallet, so WalletConnect is the only route. The option is
# hidden rather than shown-and-broken when this is unset.
VITE_WALLETCONNECT_PROJECT_ID=

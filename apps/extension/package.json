{
  "name": "apps-extension",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "build": "vite build --mode content-script && vite build --mode background && vite build --mode popup && node scripts/finalize-manifest.mjs",
    "dev": "npm run build",
    "build:dev": "EXTENSION_DEV_HOSTS=true npm run build"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^5.5.4",
    "vite": "^8.2.1"
  }
}
